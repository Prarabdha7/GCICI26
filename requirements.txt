# ---------------------------------------------------------------------------
# JA Assure AI Marketing System
# Grouped by phase.  Install everything:  pip install -r requirements.txt
# ---------------------------------------------------------------------------

# --- Phase 1: API, config, database --------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.1
SQLAlchemy>=2.0.30
python-multipart>=0.0.9
# PostgreSQL driver — only needed when DATABASE_URL points at Postgres.
psycopg[binary]>=3.2.0

# --- Phase 2: orchestration and LLM --------------------------------------
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0
google-genai>=1.0.0          # Gemini (default provider, free tier)
openai>=1.40.0               # optional alternate provider

# --- Phase 4: zero-cost video assembly -----------------------------------
edge-tts>=6.1.0
# PINNED BELOW 2.0 ON PURPOSE.  MoviePy 2.x renamed the setter API
# (set_audio -> with_audio, set_duration -> with_duration).  The pipeline
# targets the 1.x API but tolerates 2.x at runtime.  See CLAUDE.md section 8.
moviepy>=1.0.3,<2.0
imageio-ffmpeg>=0.5.1
pillow>=11.0.0  # kinetic caption cards (brand headers + disclaimers)

# --- Phase 5: research and scraping --------------------------------------
httpx>=0.27.0
beautifulsoup4>=4.12.0
# Keyless live research: DuckDuckGo search + Crawl4AI markdown scraping.
# Replaces the earlier Tavily-based path — no API key, no quota.
crawl4ai>=0.9.0
duckduckgo-search>=8.0.0

# --- Phase 6: review dashboard -------------------------------------------
jinja2>=3.1.4

# --- Phase 7: Project 2 auto-publisher -----------------------------------
APScheduler>=3.10.4

# --- Phase 8: observability (optional) -----------------------------------
# Only initialises with a valid AGENTOPS_API_KEY, and never under pytest —
# see the guard in app/main.py.
agentops>=0.4.0

# --- dev / test ----------------------------------------------------------
pytest>=8.3.0
pytest-asyncio>=0.24.0
ruff>=0.6.0
